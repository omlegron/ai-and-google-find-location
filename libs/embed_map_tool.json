{
  "name": "Map Finder",
  "description": "Cari lokasi dan tampilkan hasil dari Google Maps",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Nama tempat, misalnya 'ramen near Bandung'"
      }
    },
    "required": ["query"]
  },
  "api": {
    "url": "http://localhost:5000/search-place",
    "method": "GET",
    "response_format": "json",
    "query_param_name": "query"
  }
}
